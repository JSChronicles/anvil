schema_version: 2
targets:
- name: github-simple-repository
  provider:
    name: github
    mode: repositories
    options:
      token_env: GITHUB_TOKEN
  regions:
  - global
  include:
  - octo-org/example
  dry_run: true
  tasks:
  - name: audit_repo_security_settings
