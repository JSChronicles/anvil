schema_version: 2
targets:
  - name: aws-accounts
    provider:
      name: aws
      mode: organization
      options:
        profile: payer-profile
    regions: [us-east-1]
    tasks:
      - name: get_aws_inline_policies
        metadata:
          types: [user, role, group]
