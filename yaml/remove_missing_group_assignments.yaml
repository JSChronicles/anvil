schema_version: 2
targets:
- name: my-org
  provider:
    name: aws
    mode: organization
    options:
      profile: payer-profile
  regions:
  - us-east-1
  tasks:
  - name: remove_missing_group_assignments
  metadata:
    identity_center_region: us-west-2
