schema_version: 2
targets:
- name: prod
  provider:
    name: aws
    mode: organization
    options:
      profile: prod-root
  regions:
  - us-east-1
  tasks:
  - name: compare_asg_to_cluster_instances
  metadata:
    clusters:
    - cluster1-services
    - cluster2-services
    ecs_region: us-west-2
